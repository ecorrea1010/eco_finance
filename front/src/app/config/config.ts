import { Component } from '@angular/core';
import { Types } from '../types/types';

@Component({
  selector: 'app-config',
  standalone: true,
  imports: [Types],
  templateUrl: './config.html',
  styleUrl: './config.css',
})
export class Config {

}

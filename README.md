# eco_finance
Personal income and expense management system


Terminas de trabajar → docker-compose stop

Mañana continúas → docker-compose start

Cambias Dockerfile → docker-compose down && docker-compose up --build